# Screenshots

These image files are referenced by the top-level `README.md`. Add them here (captured from the
demo workspace, which uses fictional data — no real client information):

- **`opportunity-board.png`** — the Opportunity board (problems ranked by demand × fit × payability)
- **`landscape-map.png`** — the Map view (the landscape as a graph)

Until they're added, the README's Screenshot section shows their alt text.
